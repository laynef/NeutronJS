module.exports = (req, res, docs=false) => {

    /*

    // To Import Controllers in Routes

    // const C = require('./controllers');
    // { controller: C.v1.example.get } // => Your controller function

    Example:

    User.findAll()
    .then((response) => {
        if (docs) return response || {};
        else res.status(200).send(response);
    })
    .catch((error) => {
        res.status(400).send({ error })
    })


    */

};